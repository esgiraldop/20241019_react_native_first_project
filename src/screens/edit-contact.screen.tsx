import React from 'react';
import {Text, View} from 'react-native';

export function EditContactScreen(): React.JSX.Element {
  return (
    <View>
      <Text>This is the screen for editing a contact</Text>
    </View>
  );
}
